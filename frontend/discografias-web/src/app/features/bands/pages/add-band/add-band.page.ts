import { Component } from '@angular/core';
import { Button } from '../../components/button/button';

@Component({
  selector: 'app-add-band',
  imports: [Button],
  templateUrl: './add-band.page.html',
  styleUrl: './add-band.page.scss',
})
export class AddBand {}
